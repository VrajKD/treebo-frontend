import styled from "styled-components";

export const PoliciesContainer = styled.ul`
  // width: 100%;
  margin: 0;
  list-style-position: inside;
  padding-left: 1em;
  list-style-type: square;
`;

export const PropertyText = styled.li`
  padding: 0.35em 0;
  font-size: 0.85em;
  margin: 1.25em 0;
  color: darkslateblue;
`;
